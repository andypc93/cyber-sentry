# Cyber-Sentry

- Necessary Python Libraries:
    pip install scikit-learn
    pip install numpy
    pip install pandas
    pip install matplotlib
    pip install seaborn
    pip install sqlite3
    pip install joblib